/**
 * Math.min(value, value)
 * Math.max(value, value)
 * 
 * Math.PI 
 * 
 * // Absolute value
 * Math.abs
 * 
 * // Raises 2 to the 5th power
 * Math.pow(2,5)
 * 
 * // Remove decimal
 * Math.floor(3.99999);
 * 
 * // Round the number
 * Math.round(4.5)
 * 
 * Math.hypot(dx, dy);   // Math.sqrt(dx * dx + dy * dy);
 * 
 * Math.random()
 */

// Random integer in 1~10
const randomNum = Math.floor(Math.random() * 10) + 1;
console.log(randomNum);
