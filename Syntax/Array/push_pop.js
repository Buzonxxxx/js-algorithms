// push: add to the end of an array
// pop:  remove the last item of an array
// (faster than shift/unshift)
const arr = [1, 2, 3];
arr.push(4);
console.log(arr);
// [1,2,3,4]

arr.pop();
console.log(arr);
// [1,2,3]
